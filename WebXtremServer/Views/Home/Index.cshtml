@{
    ViewData["Title"] = "Home";
    var nodeinfo = ViewData["NodeInfo"] as NodeInfo;
}

<div class="text-center">
   
   
    <h2>htttp://{server:port}/Xterm?server=${server}&port=${port} 其server可选，默认server=当前页面地址</h2>
    <h2>例如：http://localhost:5172/Xterm?server=192.168.0.140&port=30042</h2>
</div>
